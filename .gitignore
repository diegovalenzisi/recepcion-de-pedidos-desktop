# Dependencias
node_modules/
resources/facturacion/node_modules/
backend/node_modules/

# Builds / instaladores
dist/
build/
release/
out/
*.exe
*.msi
*.zip
*.7z
*.nsis.7z

# Logs
*.log
logs/

# Variables de entorno
.env
.env.*
backend.env
!.env.example
!*.env.example

# Facturación / AFIP / ARCA - NO SUBIR
*.key
*.crt
*.pem
*.pfx
TA.xml
serviceAccount.json
serviceAccountKey.json
*serviceAccount*.json
facturacion-config.json

# Mercado Pago - NO SUBIR
mp-accounts.json
mercadopago-config.json

# Configuración local
local-config.json
config/local-config.json

# Carpetas locales sensibles
facturacion/ri/cert/
facturacion/mono/
mercadopago/

# Sistema
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude Code (configuración local, no subir)
.claude/
fixtures-resultados.json

# Artefactos y trabajo LOCAL (no son fuente ni se distribuyen)
backups-migracion/
diagnostics/

# Arte original del icono. La app usa build/icono-dlv.ico, que se genera aparte
# y vive en build/ (ya ignorado). El PNG suelto de la raiz no lo consume nadie.
/icono dlv.png

# COPIA OBSOLETA de electron/main.js que quedo en la raiz (2193 lineas contra
# 3162). package.json apunta a electron/main.js y nadie importa esta. Se ignora
# para no volver a confundirla con la real; se puede borrar del disco.
/main.js
